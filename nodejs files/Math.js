//Default export
export default function add(a, b) {
  return a + b;
}

//Named Export: For this name of the import should be same as export
export function sub(a, b) {
  return a - b;
}

//Dynamic export:
export function mult(a, b) {
  return a * b;
}
